#!/bin/bash

help() {
  echo "Usage:"
  echo "  zmachine vscode kill [options...]"
}

core() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  vscode::kill
}

run() {
  core $@
}

run $@
