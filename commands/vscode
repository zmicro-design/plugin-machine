#!/bin/bash

help() {
  echo "Usage:"
  echo "  zmachine vscode <args...>"
  echo ""
}

core() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  zvscode $@
}

run() {
  core $@
}

run $@
